---
name: xiaohongshu-skill
description: 创作面向 AI 产品经理新手和年轻读者的小红书 AI 概念词解、知识片段与流程图解笔记。当用户说“我要写一篇小红书”“我想写一篇小红书”或“写一篇小红书”时触发，也适用于用户提供关键词、长文本、文章、文件、截图或混合素材的创作任务。当用户没有选题或想法不足时，主动进行多轮、每轮少量的需求提问，收集足够信息后再搜索核验并推荐 3 个选题；选题确认后依次完成框架确认、每两张分批出图、修改验收、发布文案、小红书站内预览和人工最终发布交接。
---

# 小红书Skill

## 总则

把任务当作“选题与内容产品设计”，不要把用户素材直接吞读后改写。先判断最值得发布的重点，再逐级确认。保持可爱、年轻、故事化，但知识结论必须严谨可信。

目标读者是刚转行的 AI 产品经理小白和对 AI 感兴趣的年轻人。系列内容从基础科普逐步推进到体系、方法、产品应用与资深 AI 产品经理判断力。

开始工作前按需读取：

- 选题、检索、内容脚本：`references/content-research.md`
- 版式、字体、固定 IP 与视觉验收：`references/visual-system.md`
- 登录、发布方式、站内预览与交接：`references/publishing-handoff.md`

## 强制确认流程

任何阶段都不得跨过尚未完成的确认节点。

### 1. 判断是否需要选题访谈

用户说“我要写一篇小红书”“我想写一篇小红书”或“写一篇小红书”时立即进入本流程。

如果用户没有明确主题、没有素材或只说“没想法”，不要在第一轮直接推荐选题。主动进行多轮访谈，每轮只问 1 至 3 个最能推进决策的问题，等待回答后总结“已知信息”和“仍缺信息”，再进入下一轮。

至少收集以下 6 项中的 4 项，且必须包含“方向/问题”和“读者收益”，才视为信息足够：

- 最近关注的 AI 方向、工作问题或学习卡点
- 想让读者看完获得什么
- 可使用的经历、案例、观点或素材
- 本篇位于系列的哪个阶段
- 希望偏概念、流程、误区、案例还是产品判断
- 不想讲什么、篇幅或表达边界

详细提问方法见 `references/content-research.md`。信息足够后才进入检索和 3 个选题推荐。

### 2. 接收并甄别素材

接受关键词、长文本、文章、文件、截图或多种模态同时输入。识别素材中的核心事实、可发布观点、读者价值、潜在误区和系列位置。

如用户只给术语或知识片段，也要先分析“这次最适合讲什么”，不要默认做百科式词解。

### 3. 先检索，再澄清含义

按照 `references/content-research.md` 完成搜索与交叉验证。优先原始论文、标准、官方文档和权威教育资料。

发现一词多义、具体指向不清或不同语境结论不同，立即询问用户并暂停选题。例如 `ANN` 既可能指人工神经网络，也可能指近似最近邻检索；未确认含义时不得继续。

### 4. 给出 3 个选题供选择

每个选题写清：

- 推荐标题或切角
- 核心读者收益
- 会讲什么、不讲什么
- 建议页数
- 在系列中的位置

给出明确推荐，但等待用户选择后再规划页面。

### 5. 规划完整框架并等待确认

围绕已选题目规划总页数，至少 4 张，默认优先考虑 6 张；知识较多时可增加，但必须先确认张数。

逐页列出：页面任务、核心结论、文字结构、关系/流程、故事画面、角色是否出现。框架必须覆盖“是什么、属于哪个体系、为什么重要、解决什么问题、怎么工作或怎么用、形象例子”，并在适合时加入 AI 产品经理视角和下期关联词。

等待用户确认框架后才开始制作成图。

### 6. 每次只生成 2 张

以两张为一批制作并交付预览。每批等待用户确认或修改，不得提前生成下一批。

需要 AI 插画时使用图像生成能力；需要准确中文、流程箭头、关系框和稳定字号时，优先把插画作为素材，再用确定性排版方式合成文字和结构，避免让图像模型直接承担大量中文排版。

### 7. 按反馈修订并锁定版本

只修改用户指出的页面和问题，保留已确认内容。常见检查项包括：封面标题大小、导读位置、正文信息密度、关系表达、黑色正文字号、边距、框线、角色遮挡和页面一致性。

每次修订后使用新版本目录，确认最终文件夹只含最新 1 至 N 张成图，避免混入旧版。运行：

```bash
python3 scripts/validate_image_set.py "/absolute/path/to/final-images" --keyword "ANN"
```

### 8. 生成发布文案

提供 1 个推荐标题和 2 个备选标题。正文不超过 200 个中文字符，必须与图片内容一致；补充 3 至 5 个相关话题。不得靠夸张承诺、错误类比或标题党弥补内容不足。

### 9. 生成小红书站内预览

仅在用户要求进入发布环节时，读取 `references/publishing-handoff.md` 并执行。上传前再次核对最终图片目录，填写标题、正文、话题和用户选择的发布方式。

生成站内预览并交给用户确认。最终红色“发布”按钮永远由用户本人点击；本 Skill 不代替用户完成最后提交。

## 交付清单

每篇最终应包含：

- 选题结论与核验依据
- 已确认的页数和逐页框架
- 按顺序编号的 3:4 成图，至少 4 张
- 1 个推荐标题、2 个备选标题
- 不超过 200 字的正文和话题建议
- 小红书站内预览或清晰的发布交接状态

不要把内部检索笔记、临时图片、旧版本或账号凭据放进最终交付。
